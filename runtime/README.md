# runtime

Local runtime bundle workspace.

- `bin/llama_main.exe`
- `models/*.pte`
- `tokenizer/tokenizer.json`
- `prompts/perf_prompt.txt`

This directory is partially gitignored to avoid committing large binaries/models.
